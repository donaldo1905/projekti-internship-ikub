import { SaveMovieDirective } from './save-movie.directive';

describe('SaveMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveMovieDirective();
    expect(directive).toBeTruthy();
  });
});
